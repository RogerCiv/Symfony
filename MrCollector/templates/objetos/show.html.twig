{% extends 'base.html.twig' %}

{% block title %}Objetos{% endblock %}

{% block body %}
    <h1>Objetos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objeto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ objeto.nombre }}</td>
            </tr>
            <tr>
                <th>Foto</th>
                <td>{{ objeto.foto }}</td>
            </tr>
            <tr>
                <th>Precio_salida</th>
                <td>{{ objeto.precioSalida }}</td>
            </tr>
            <tr>
                <th>Precio_estimado_actual</th>
                <td>{{ objeto.precioEstimadoActual }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_objetos_index') }}">back to list</a>

    <a href="{{ path('app_objetos_edit', {'id': objeto.id}) }}">edit</a>

    {{ include('objetos/_delete_form.html.twig') }}
{% endblock %}
