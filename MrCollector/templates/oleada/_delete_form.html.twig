<form method="post" action="{{ path('app_oleada_delete', {'id': oleada.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oleada.id) }}">
    <button class="btn">Delete</button>
</form>
